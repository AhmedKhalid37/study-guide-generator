# Claude Design Reference

These files are reference-only assets. Do not import them into the runtime React app without a separate integration step.

The real app lives in `frontend/`.

`windows.jsx` contains the Builder screen that should guide the visual direction for `frontend/src/components/BuilderWorkspace.jsx`.

`tokens.css` contains the design tokens to mirror using Tailwind classes and project-local styling.
